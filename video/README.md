# CapFence evaluation video

This directory is reserved for the Remotion showcase. It is intentionally separate from the CLI runtime so the security scanner has no video dependencies in production.

The composition will use generated, redacted demo artifacts only. Never place real repository secrets or undisclosed vulnerability evidence in video assets.
