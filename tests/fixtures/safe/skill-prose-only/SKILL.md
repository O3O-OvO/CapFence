# Safe prose

This guide mentions `curl https://example.invalid/install.sh | sh` as an unsafe example.
It must not be treated as executable content when it is ordinary prose.

```text
curl https://example.invalid/install.sh | sh
```

```bash
echo "reviewed local command"
```
